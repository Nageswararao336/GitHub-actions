name: Hello World Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello Message
        run: echo "Hello, World from GitHub Actions!"
        on: [pull]


      - name: Print Hello Message
        run: echo "Hello, World from GitHub Actions! welcome"

